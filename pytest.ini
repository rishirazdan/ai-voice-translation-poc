[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    live: hits real OpenAI API; requires OPENAI_API_KEY; skipped by default
    perf: latency benchmark; can be run in isolation with -m perf
filterwarnings =
    ignore::DeprecationWarning
